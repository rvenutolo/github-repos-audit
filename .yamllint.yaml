extends: default

rules:
  # `on:` is a GitHub Actions key, not a boolean.
  truthy:
    check-keys: false
  document-start: disable
  # prettier owns formatting and puts one space before an inline comment.
  comments:
    min-spaces-from-content: 1
  line-length:
    max: 120
